#include <stdio.h>

int main() {
    //Declaração das variaveis
float r,h,v;  
 printf("Digite o valor do raio:");
scanf(“%f”,&r);
printf("Digite o valor da altura:");
    scanf(“%f”,&h);
    // Calculo do volume
   v = 3.14159 * (r*r) * h;
printf(“Valor do Volume:%.4f”,v);
    return 0;
}
